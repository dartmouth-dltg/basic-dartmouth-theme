<?php
$translate = $this->plugin('translate');
$prevText = $translate('Prev');
$nextText = $translate('Next');
?>
<?php if ($prevPage || $nextPage): ?>
<div class="site-page-pagination">
    <?php echo $prevPage ? $this->hyperlink($prevText, $prevPage->siteUrl(), ['class' => 'site-pagination-prev btn btn-primary']) : ''; ?>
    <?php echo $nextPage ? $this->hyperlink($nextText, $nextPage->siteUrl(), ['class' => 'site-pagination-next btn btn-primary']) : ''; ?>
</div>
<?php endif; ?>